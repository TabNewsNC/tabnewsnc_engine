# Changelog

## 0.1.0

* Added API constants
* Added the news entity
* Added news filter enum
* Added the http service with methods: `getOne` and `getAll`
* Added the news repository with the method: `getAll` (to get news by filter)
